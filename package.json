{
  "name": "radicle-reclaim",
  "version": "0.0.0",
  "private": true,
  "description": "Reclaim: an 835 (ERA) feeder + denial-analytics build for Radicle Health's AccuBill.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  }
}
