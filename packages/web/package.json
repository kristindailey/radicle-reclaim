{
  "name": "web",
  "version": "0.0.0",
  "private": true,
  "description": "Vue denial-analytics dashboard: a thin reader over one reconcile() result.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "pretypecheck": "pnpm --filter core build",
    "typecheck": "vue-tsc --noEmit -p tsconfig.json",
    "pretest": "pnpm --filter core build",
    "test": "jest"
  },
  "dependencies": {
    "core": "workspace:*",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.14.0",
    "@vitejs/plugin-vue": "^5.2.1",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.0",
    "typescript": "^5.6.2",
    "vite": "^6.0.7",
    "vue-tsc": "^2.2.0"
  }
}
