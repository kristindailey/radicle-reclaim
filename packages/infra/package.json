{
  "name": "infra",
  "version": "0.0.0",
  "private": true,
  "description": "Pulumi TypeScript stack for Reclaim: S3 ingest, Lambda, DynamoDB, AppSync.",
  "main": "dist/index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "jest",
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  },
  "dependencies": {
    "@pulumi/pulumi": "^3.261.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.14.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.0",
    "typescript": "^5.6.2"
  }
}
